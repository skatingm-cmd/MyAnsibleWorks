---
- name: Install containerd
  ansible.builtin.package:
    name: containerd.io
    state: present

- name: Enable and start containerd (initial)
  ansible.builtin.systemd:
    name: containerd
    enabled: yes
    state: started
