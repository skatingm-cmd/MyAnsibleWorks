---
- name: Install chrony
  ansible.builtin.dnf:
    name: chrony
    state: present

- name: Enable and start chrony
  ansible.builtin.systemd:
    name: chronyd
    enabled: yes
    state: started
